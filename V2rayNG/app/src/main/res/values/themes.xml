<resources>



    <!-- 🌈 تم اصلی برنامه (فقط لایت) -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- نوار وضعیت و ناوبری شفاف و زیبا -->
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>

        <!-- کنترل‌های زیباتر -->
        <item name="colorControlNormal">@color/colorAccent</item>
        <item name="colorControlActivated">@color/colorPrimary</item>
    </style>

    <!-- ⬆️ نسخه بدون اکشن‌بار -->
    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- ✨ نسخه ترنسپرنت برای صفحات خاص (مثل اسپلش یا دیالوگ‌های تمام‌صفحه) -->
    <style name="AppTheme.NoActionBar.Translucent" parent="AppTheme.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
    </style>

    <!-- 🔘 سوئیچ برندینگ با رنگ جذاب -->
    <style name="BrandedSwitch" parent="Widget.AppCompat.CompoundButton.Switch">
        <item name="colorAccent">@color/colorFabActive</item>
    </style>

</resources>
